# 二轮充电接口说明 v2

## 目录

- [1. 基础约定](#1-基础约定)
- [2. 枚举和公共字段](#2-枚举和公共字段)
- [3. 接口目录](#3-接口目录)
- [4. 后台提交接口](#4-后台提交接口)
- [5. 小程序业务接口](#5-小程序业务接口)
- [6. 支付接口](#6-支付接口)
- [7. 数据库表说明](#7-数据库表说明)
- [8. 接口实现状态检查](#8-接口实现状态检查)
- [9. 当前代码和数据库核对](#9-当前代码和数据库核对)

## 1. 基础约定

二轮充电是独立模块，接口使用 `/twoWheel/*`、小程序接口使用 `/twoWheel/xcx/*`、支付接口使用 `/pay/twoWheel*`。二轮模块不复用四轮汽车充电的 `/pay/charging` 和 `charging_*` 业务表。

`BKV` 只是设备品牌，写入 `device_brand='BKV'`。当桩或枪口为 BKV 时，启动和停止会调用 `barrier_gate_system` 的 BKV 中间件。

通用成功响应：

```json
{
  "success": true,
  "data": {}
}
```

通用失败响应：

```json
{
  "success": false,
  "error": "错误原因"
}
```

## 2. 枚举和公共字段

### 2.1 站点状态 status

| 值 | 说明 | 前端展示 |
| --- | --- | --- |
| enable | 正常收费 | 可正常扫码、下单、充电 |
| repair | 施工维护 | 站点维护中 |
| disable | 关闭 | 站点关闭 |

当前代码已对站点创建、修改、状态变更校验以上三个值。

### 2.2 三种收费模式

| 字段 | 模式值 | 说明 |
| --- | --- | --- |
| duration_strategy_id | duration | 按时长收费 |
| electricity_service_strategy_id | electricity_service | 电价服务费分离 |
| per_use_strategy_id | per_use | 按次收费 |

站点表最终写入字段为 `two_wheel_strategy_ids`，格式如下：

```json
{
  "duration": "STRATEGY_ID_1",
  "electricity_service": "STRATEGY_ID_2",
  "per_use": "STRATEGY_ID_3"
}
```

创建或修改站点时，可以直接传 `two_wheel_strategy_ids`，也可以传上面的三个友好字段。代码会自动归一化。

收费模式业务流程：

1. 后台先通过 `/twoWheel/strategy/create` 创建三类策略，`type` 分别为 `duration`、`electricity_service`、`per_use`。
2. 后台创建或修改站点时，把策略 ID 绑定到站点。可以传 `duration_strategy_id`、`electricity_service_strategy_id`、`per_use_strategy_id`，也可以直接传 `two_wheel_strategy_ids`。
3. 小程序扫码后展示站点可用收费模式。可用模式以站点 `two_wheel_strategy_ids` 中存在的 key 为准。
4. 小程序创建订单或支付充电订单时，传 `charging_mode` 指定本次使用的收费模式。可选值为 `duration/electricity_service/per_use`。如前端已经明确策略，也可以同时传 `strategy_id`；后端应校验该策略属于当前站点对应模式。

创建站点时的策略绑定示例：

```json
{
  "station_no": "TWS001",
  "name": "东门二轮充电站",
  "status": "enable",
  "duration_strategy_id": "STRATEGY_DURATION_ID",
  "electricity_service_strategy_id": "STRATEGY_ES_ID",
  "per_use_strategy_id": "STRATEGY_PER_USE_ID"
}
```

小程序下单或支付时的模式选择示例：

```json
{
  "two_wheel_station_id": "STATION_ID",
  "two_wheel_port_id": "PORT_ID",
  "charging_mode": "duration",
  "strategy_id": "STRATEGY_DURATION_ID"
}
```

### 2.3 白名单开关

站点字段 `whitelist_apply` 表示是否启用白名单免支付策略：

| 值 | 说明 |
| --- | --- |
| true | 当前站点参与白名单免支付判断 |
| false | 当前站点不使用白名单免支付 |

白名单真正免支付需要同时满足：站点 `whitelist_apply=true`、用户或手机号命中白名单、白名单 `status=enable`、未过期、站点适用。

## 3. 接口目录

本文按前端/小程序实际对接路径整理接口；站、桩、枪模块只展开提交和状态变更接口，查询接口不在本文中描述。

### 后台接口

- [POST /twoWheel/station/create](#post-twowheelstationcreate)
- [POST /twoWheel/station/update](#post-twowheelstationupdate)
- [POST /twoWheel/station/status](#post-twowheelstationstatus)
- [POST /twoWheel/station/delete](#post-twowheelstationdelete)
- [POST /twoWheel/pile/create](#post-twowheelpilecreate)
- [POST /twoWheel/pile/update](#post-twowheelpileupdate)
- [POST /twoWheel/pile/status](#post-twowheelpilestatus)
- [POST /twoWheel/pile/delete](#post-twowheelpiledelete)
- [POST /twoWheel/port/create](#post-twowheelportcreate)
- [POST /twoWheel/port/update](#post-twowheelportupdate)
- [POST /twoWheel/port/status](#post-twowheelportstatus)
- [POST /twoWheel/port/delete](#post-twowheelportdelete)
- [POST /twoWheel/strategy/create](#post-twowheelstrategycreate)
- [POST /twoWheel/strategy/update](#post-twowheelstrategyupdate)
- [POST /twoWheel/strategy/delete](#post-twowheelstrategydelete)
- [POST /twoWheel/whitelist/create](#post-twowheelwhitelistcreate)
- [POST /twoWheel/whitelist/update](#post-twowheelwhitelistupdate)
- [POST /twoWheel/whitelist/delete](#post-twowheelwhitelistdelete)
- [POST /twoWheel/monthCard/config/create](#post-twowheelmonthcardconfigcreate)
- [POST /twoWheel/monthCard/config/update](#post-twowheelmonthcardconfigupdate)
- [POST /twoWheel/monthCard/config/delete](#post-twowheelmonthcardconfigdelete)
- [POST /twoWheel/monthCard/create](#post-twowheelmonthcardcreate)
- [POST /twoWheel/monthCard/update](#post-twowheelmonthcardupdate)
- [POST /twoWheel/monthCard/delete](#post-twowheelmonthcarddelete)
- [POST /twoWheel/rechargeCard/create](#post-twowheelrechargecardcreate)
- [POST /twoWheel/rechargeCard/update](#post-twowheelrechargecardupdate)
- [POST /twoWheel/rechargeCard/recharge](#post-twowheelrechargecardrecharge)
- [POST /twoWheel/rechargeCard/return](#post-twowheelrechargecardreturn)
- [POST /twoWheel/rechargeCard/logs](#post-twowheelrechargecardlogs)
- [POST /twoWheel/rechargeCard/delete](#post-twowheelrechargecarddelete)

### 小程序接口

- [POST /twoWheel/xcx/order/create](#post-twowheelxcxordercreate)
- [POST /twoWheel/xcx/startCharge](#post-twowheelxcxstartcharge)
- [POST /twoWheel/xcx/stopCharge](#post-twowheelxcxstopcharge)

### 支付接口

- [POST /pay/twoWheelCharging](#post-paytwowheelcharging)
- [POST /pay/twoWheelMonthCard](#post-paytwowheelmonthcard)
- [POST /pay/twoWheelChargingCard](#post-paytwowheelchargingcard)

## 4. 后台提交接口

### POST /twoWheel/station/create

创建二轮充电站。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电站创建 | v2 二轮充电站创建 |
| --- | --- | --- |
| 前端接口 | `POST /charging/station/new` | `POST /twoWheel/station/create` |
| 数据落点 | `charging_station` | `two_wheel_station` |

原四轮创建逻辑的核心流程：

1. 校验必填字符串：`charging_station_no/name/data_owner_id/address/province/city/district/status`。
2. 对可选字段补默认值：例如 `parking_lot_id/tag/property_* /area/comment` 置空，`images/infrastructure_tags` 默认空数组。
3. 初始化扩展字段：`options` 默认包含 `{ "hlht": false, "logic_status": "enable" }`。
4. 使用站点名称 `name` 做 `findOrCreate` 防重复，创建成功后写入 `charging_station`，并记录后台操作日志。

二轮 v2 已对齐后的实现流程：

1. 先归一化请求字段：经纬度转数字，桩数/插孔数转整数，`whitelist_apply/map_induction/invoice_apply` 等转布尔值，`tag/tags/images/infrastructure_tags` 转数组，`two_wheel_strategy_ids/options` 转对象。
2. 校验必填字段：`station_no` 或 `two_wheel_station_no`、`name/data_owner_id/address/province/city/district/status`。
3. 校验 `status` 必须仍为四轮含义的 `enable/repair/disable`。
4. 把 `duration_strategy_id/electricity_service_strategy_id/per_use_strategy_id` 组装进 `two_wheel_strategy_ids`。
5. 对可选字段补默认值：`parking_lot_id/property_* /area/comment/remark` 置空，`tag/tags/images/infrastructure_tags` 默认空数组。
6. 初始化扩展字段：`options` 默认包含 `{ "hlht": false, "logic_status": "enable" }`，同时保留前端传入的其他扩展字段。
7. 使用站点名称 `name` 做 `findOrCreate` 防重复，创建成功后写入 `two_wheel_station`。
8. 如果当前后台会话存在登录用户，则记录 `create_two_wheel_station` 操作日志。

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 仍然按“站点主数据”创建，保留站点编号、名称、归属、车场、地址、省市区、坐标、标签、图片、运营说明、发票、白名单开关、扩展 `options` 等旧模型能力。 |
| 继承 | 当前实现已经按四轮创建流程补齐：必填校验、可选字段默认值、`options.logic_status`、按 `name` 防重复、创建日志。 |
| 继承 | 仍然为后续桩、枪、订单和计费策略提供站点主键；前端创建站点后，应使用返回的 `data.id` 作为二轮桩和枪的 `two_wheel_station_id`。 |
| 继承 | 旧四轮 `charging_pile_num/charging_port_num` 的统计口径保留，但二轮实际数量以后续 `two_wheel_pile/two_wheel_port` 为准，可由后台统计回填。 |
| 继承 | 状态继续沿用四轮含义和枚举：`enable` 正常收费，`repair` 施工维护，`disable` 关闭。 |
| 修改 | 接口名从 `/charging/station/new` 换为 `/twoWheel/station/create`，前端不要再调用旧四轮站点接口。 |
| 修改 | 数据不再写 `charging_station`，只写 `two_wheel_station`；后续二轮订单、支付、BKV 回调也只关联 `two_wheel_*`。 |
| 修改 | `charging_station_no` 拆换为 `station_no/two_wheel_station_no`：`station_no` 是二轮业务编号，`two_wheel_station_no` 是外部/设备侧兼容编号；不传时默认等于 `station_no`。 |
| 修改 | 四轮单策略 `charging_strategy_id` 扩展为二轮多策略 `two_wheel_strategy_ids`，支持按时长、度电+服务费、按次三种二轮计费模式。 |
| 新增 | 增加 `device_brand`，默认 `BKV`，用于后续区分 BKV 二轮设备指令和回调处理。 |
| 新增 | 增加 `service_phone/business_hours/remark/tags` 等更适合小程序展示的字段。 |

字段映射：

| 四轮旧字段 | 二轮 v2 字段 | 说明 |
| --- | --- | --- |
| `charging_station_no` | `station_no` | 二轮站点业务编号，必填。 |
| `charging_station_no` | `two_wheel_station_no` | 二轮外部编号，选填；不传默认等于 `station_no`。 |
| `name` | `name` | 沿用站点名称。 |
| `data_owner_id/parking_lot_id` | `data_owner_id/parking_lot_id` | 沿用归属和停车场关联。 |
| `address/province/city/district/longitude/latitude` | 同名字段 | 沿用地址和坐标。 |
| `status=enable` | `status=enable` | 正常收费/可用，沿用四轮含义。 |
| `status=repair` | `status=repair` | 施工维护，沿用四轮含义。 |
| `status=disable` | `status=disable` | 关闭，沿用四轮含义。 |
| `tag/infrastructure_tags/images` | `tag/infrastructure_tags/images/tags` | 保留旧标签和图片，同时新增 `tags` 便于小程序展示。 |
| `charging_strategy_id` | `duration_strategy_id/electricity_service_strategy_id/per_use_strategy_id` 或 `two_wheel_strategy_ids` | 从四轮单策略变为二轮多策略映射。 |
| `whitelist_apply` | `whitelist_apply` | 继续表示是否启用白名单。 |
| `operating_description/comment/options` | `operating_description/comment/options/remark` | 保留扩展说明，新增 `remark` 作为前端备注。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| station_no | string | 是 | 站点编号 |
| name | string | 是 | 站点名称 |
| two_wheel_station_no | string | 否 | 二轮外部编号；不传默认等于 `station_no` |
| data_owner_id | string | 是 | 数据归属，沿用四轮必填规则 |
| parking_lot_id | string | 否 | 关联停车场 |
| address | string | 是 | 参考地址/站点地址 |
| province | string | 是 | 省 |
| city | string | 是 | 市 |
| district | string | 是 | 区县 |
| longitude | number | 否 | 经度 |
| latitude | number | 否 | 纬度 |
| status | string | 是 | 运营/使用状态：`enable` 正常收费，`repair` 施工维护，`disable` 关闭 |
| tag | array | 否 | 站点标签 |
| tags | array | 否 | 站点标签，和 `tag` 均可用于展示 |
| whitelist_apply | boolean | 否 | 是否启用白名单策略；默认 `false` |
| duration_strategy_id | string | 否 | 按时长策略 ID |
| electricity_service_strategy_id | string | 否 | 电价分离策略 ID |
| per_use_strategy_id | string | 否 | 按次策略 ID |
| two_wheel_strategy_ids | object | 否 | 三种策略映射；可替代上面三个策略字段 |
| charging_pile_num | number | 否 | 充电桩数量；创建后可由桩表统计回填 |
| charging_port_num | number | 否 | 插孔数量；创建后可由枪口表统计回填 |
| device_brand | string | 否 | 默认品牌，默认 `BKV` |
| service_phone | string | 否 | 服务电话 |
| business_hours | string | 否 | 营业时间 |
| images | array | 否 | 站点图片 |
| operating_description | string | 否 | 运营说明 |
| remark | string | 否 | 备注 |
| comment | string | 否 | 说明 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "station_no": "TWS001",
  "name": "东门二轮充电站",
  "data_owner_id": "DATA_OWNER_ID",
  "parking_lot_id": "PARKING_LOT_ID",
  "address": "园区东门",
  "longitude": 119.30000000,
  "latitude": 26.08000000,
  "province": "福建省",
  "city": "福州市",
  "district": "鼓楼区",
  "tag": ["园区", "东门"],
  "status": "enable",
  "whitelist_apply": true,
  "duration_strategy_id": "STRATEGY_DURATION_ID",
  "electricity_service_strategy_id": "STRATEGY_ES_ID",
  "per_use_strategy_id": "STRATEGY_PER_USE_ID",
  "service_phone": "4000000000",
  "business_hours": "00:00-24:00",
  "remark": "东门入口旁"
}
```

响应参数：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| data.id | string | 站点主键 |
| data.station_no | string | 站点编号 |
| data.name | string | 站点名称 |
| data.status | string | 站点状态 |
| data.status_updated_at | string | 状态更新时间 |
| data.whitelist_apply | boolean | 是否启用白名单策略 |
| data.two_wheel_strategy_ids | object | 三种收费模式对应策略 |
| data.charging_pile_num | number | 充电桩数量 |
| data.charging_port_num | number | 插孔数量 |
| data.remark/comment/operating_description | string | 备注和说明 |

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "STATION_ID",
    "station_no": "TWS001",
    "name": "东门二轮充电站",
    "status": "enable",
    "status_updated_at": "2026-06-02 10:00:00",
    "whitelist_apply": true,
    "charging_pile_num": 0,
    "charging_port_num": 0,
    "two_wheel_strategy_ids": {
      "duration": "STRATEGY_DURATION_ID",
      "electricity_service": "STRATEGY_ES_ID",
      "per_use": "STRATEGY_PER_USE_ID"
    },
    "remark": "东门入口旁"
  }
}
```

### POST /twoWheel/station/update

修改二轮充电站。`update` 可以修改任意支持字段，不限于改名。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电站修改 | v2 二轮充电站修改 |
| --- | --- | --- |
| 前端接口 | `POST /charging/station/edit` | `POST /twoWheel/station/update` |
| 数据落点 | `charging_station` | `two_wheel_station` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮“按站点主键修改站点主数据”的设计，`id` 为必填，其他字段按需传入。 |
| 继承 | 状态仍使用四轮枚举 `enable/repair/disable`，含义不变。 |
| 继承 | 地址、坐标、省市区、标签、图片、运营说明、发票、白名单开关、扩展 `options` 等站点属性继续可修改。 |
| 继承 | 当前实现已对齐四轮修改保护：站点改名会校验同名；修改车场时若下属枪口已绑定车位则拒绝；修改成功写操作日志。 |
| 修改 | 接口名从 `/charging/station/edit` 换为 `/twoWheel/station/update`。 |
| 修改 | 数据不再写 `charging_station`，只更新 `two_wheel_station`。 |
| 修改 | 原四轮 `charging_strategy_id` 改为二轮 `two_wheel_strategy_ids` 多策略映射，也可以继续用三个友好字段更新。 |
| 修改 | 站点车场变更会同步更新下属 `two_wheel_pile.parking_lot_id`；这是对四轮“同步桩车场”的二轮表适配。 |
| 新增 | 可修改 `device_brand/service_phone/business_hours/remark/tags` 等二轮小程序展示字段。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 站点主键 |
| station_no | string | 否 | 站点编号 |
| name | string | 否 | 站点名称 |
| status | string | 否 | `enable/repair/disable` |
| whitelist_apply | boolean | 否 | 是否启用白名单策略 |
| duration_strategy_id | string | 否 | 按时长策略 ID |
| electricity_service_strategy_id | string | 否 | 电价分离策略 ID |
| per_use_strategy_id | string | 否 | 按次策略 ID |
| two_wheel_strategy_ids | object | 否 | 三种策略映射 |
| address/longitude/latitude | string/number | 否 | 地址坐标 |
| province/city/district | string | 否 | 省市区 |
| tag/tags | array | 否 | 站点标签 |
| charging_pile_num | number | 否 | 充电桩数量 |
| charging_port_num | number | 否 | 插孔数量 |
| service_phone/business_hours | string | 否 | 服务电话、营业时间 |
| operating_description | string | 否 | 运营说明 |
| remark | string | 否 | 备注 |
| comment | string | 否 | 说明 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "id": "STATION_ID",
  "status": "repair",
  "whitelist_apply": false,
  "duration_strategy_id": "NEW_DURATION_STRATEGY_ID",
  "remark": "施工维护，暂不开放"
}
```

响应同创建接口，返回更新后的站点记录。

### POST /twoWheel/station/status

单独修改站点状态。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电站状态修改 | v2 二轮充电站状态修改 |
| --- | --- | --- |
| 前端接口 | `POST /charging/station/updateStatusMany` | `POST /twoWheel/station/status` |
| 数据落点 | `charging_station` | `two_wheel_station` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮站点状态含义：`enable` 正常收费，`repair` 施工维护，`disable` 关闭。 |
| 继承 | 当前实现会校验状态值，修改 `status/status_updated_at`，并记录二轮站点状态操作日志。 |
| 修改 | 四轮旧接口支持批量 `ids + newValue`；二轮当前接口按单站点 `id + status` 修改。 |
| 修改 | 数据不再写 `charging_station`，只更新 `two_wheel_station.status/status_updated_at`。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 站点主键 |
| status | string | 是 | `enable/repair/disable` |

请求示例：

```json
{
  "id": "STATION_ID",
  "status": "disable"
}
```

响应同创建接口。

### POST /twoWheel/station/delete

删除二轮充电站。后端按软删除处理，记录会写入 `deleted=true` 和 `deleted_at`。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电站删除 | v2 二轮充电站删除 |
| --- | --- | --- |
| 前端接口 | `POST /charging/station/delete` | `POST /twoWheel/station/delete` |
| 数据落点 | `charging_station` | `two_wheel_station` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮软删除设计，删除后记录保留并写入删除标记和删除时间。 |
| 修改 | 接口名从 `/charging/station/delete` 换为 `/twoWheel/station/delete`。 |
| 修改 | 数据不再写 `charging_station`，只删除 `two_wheel_station`。 |
| 修改 | 二轮新增删除保护：站点下仍存在未删除桩、枪或未完成订单时拒绝删除；删除成功写二轮站点删除日志。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 站点主键 |

请求示例：

```json
{
  "id": "STATION_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "STATION_ID"
  }
}
```

### POST /twoWheel/pile/create

创建二轮充电桩。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电桩创建 | v2 二轮充电桩创建 |
| --- | --- | --- |
| 前端接口 | `POST /charging/pile/create` | `POST /twoWheel/pile/create` |
| 数据落点 | `charging_pile`、`charging_port` | `two_wheel_pile`、`two_wheel_port` |

原四轮创建逻辑的核心流程：

1. 校验桩必填字段：`device_no/device_name/type/brand/using_status/data_owner_id`。
2. 校验设备厂商规则，例如部分厂家限制桩编号长度和纯数字格式。
3. 校验 `using_status`，四轮旧逻辑中 `start` 表示已关联站点可用，`stock` 表示库存。
4. 校验 `charging_station_id`：启用状态必须关联站点，库存状态不能关联站点。
5. 校验 `charging_ports` 数组，枪口编号需按“桩编号 + 两位枪口序号”规则生成，枪口状态只能为 `enable/disable`。
6. 在事务内先写 `charging_pile`，再批量写 `charging_port`，并记录后台操作日志。

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 仍然沿用“创建桩时可同步创建枪口”的设计，`ports` 对应四轮旧接口的 `charging_ports`。 |
| 继承 | 桩启停状态继续使用 `pile_status=enable/disable`；枪口启停状态继续使用 `port_status=enable/disable`。 |
| 继承 | 枪口数量仍按下属枪口数量维护，创建时可由 `ports` 数组自动计算。 |
| 继承 | 当前实现已补齐四轮核心校验：站点必须存在、设备号不能重复、同批端口号和端口设备号不能重复、桩/枪状态值必须合法。 |
| 继承 | 当前实现已补齐四轮事务和日志口径：创建桩和随桩创建端口在同一事务内完成，创建成功后写二轮桩/枪操作日志。 |
| 修改 | 接口名从 `/charging/pile/create` 换为 `/twoWheel/pile/create`。 |
| 修改 | 数据不再写 `charging_pile/charging_port`，只写 `two_wheel_pile/two_wheel_port`。 |
| 修改 | 四轮 `charging_station_id` 改为 `two_wheel_station_id`；返回和后续接口也使用二轮站点主键。 |
| 修改 | 四轮 `charging_ports` 改为 `ports`，二轮支持 `port_no/port_id`，BKV 0 号口必须传字符串 `"00"`。 |
| 修改 | 四轮 `using_status=start/stock` 在二轮接口中使用 `using_status=using/stock`，更贴近设备管理页面展示。 |
| 新增 | 增加 `device_mac/device_brand/brand_model/device_status/operator_name/signal_strength/device_temperature/remote_control_enabled/realtime_query_enabled`，用于 BKV 设备管理和小程序展示。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| two_wheel_station_id | string | 是 | 所属站点 ID |
| device_no | string | 是 | 设备号；同时作为设备 UID 使用，不另设 `device_uid` |
| device_name | string | 是 | 设备名称；后端同时写入 `name` |
| brand_model | string | 是 | 品牌型号；例如 `BKV-10K` |
| using_status | string | 是 | 设备状态：`using` 使用中，`stock` 库存 |
| device_mac | string | 否 | 设备 MAC；不传默认取 `device_no` |
| pile_no | string | 否 | 桩编号；不传默认取 `device_no` |
| name | string | 否 | 桩名称；不传默认取 `device_name` |
| device_brand | string | 否 | 设备品牌，默认 `BKV` |
| brand | string | 否 | 品牌展示名；可与 `brand_model` 配合展示 |
| type | string | 否 | `AC/DC/AC_DC/other`，默认 `AC` |
| device_type | string | 否 | 设备类型，默认取 `type` |
| pile_status | string | 否 | `enable/disable`，默认 `enable` |
| device_status | string | 否 | 在线状态：`online/offline/fault/configuring`，默认 `offline`；心跳约 10 分钟更新一次 |
| charging_port_num | number | 否 | 插孔数量；不传时按 `ports` 数组长度计算 |
| qrcode_id | string | 否 | 二维码 ID |
| operator_id | string | 否 | 运营方 ID |
| operator_name | string | 否 | 运营方名称，用于列表搜索/展示 |
| signal_strength | number | 否 | 4G 信号等级 |
| device_temperature | number | 否 | 设备温度 |
| remote_control_enabled | boolean | 否 | 是否支持远程开启/关闭充电，默认 `true` |
| realtime_query_enabled | boolean | 否 | 是否支持远程查询实时状态，默认 `true` |
| ports | array | 否 | 创建桩时同步创建下属充电插口 |
| ports[].port_id | string | 否 | 充电口业务 ID；未传 `port_no` 时作为口编号使用 |
| ports[].port_no | string | 否 | 充电口编号；BKV 0 号端口传 `"00"` |
| ports[].power | number | 否 | 充电口功率；后端写入 `max_power` |
| ports[].max_power | number | 否 | 充电口最大功率 |
| ports[].status | string | 否 | 充电口状态；后端写入 `port_status`，默认 `enable` |
| ports[].port_status | string | 否 | `enable/disable` |
| ports[].using_status | string | 否 | `free/charging/fault`，默认 `free` |
| ports[].remark | string | 否 | 插口备注 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "two_wheel_station_id": "STATION_ID",
  "device_no": "BKV001",
  "device_name": "东门1号二轮桩",
  "device_mac": "A1:B2:C3:D4:E5:F6",
  "device_brand": "BKV",
  "brand_model": "BKV-10K",
  "pile_status": "enable",
  "using_status": "using",
  "operator_id": "OPERATOR_ID",
  "operator_name": "丰泽运营",
  "remark": "东门第一台",
  "ports": [
    {
      "port_id": "00",
      "power": 0.8,
      "status": "enable"
    },
    {
      "port_id": "01",
      "power": 0.8,
      "status": "enable"
    }
  ]
}
```

响应参数：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| data.pile | object | 创建后的 `two_wheel_pile` 记录 |
| data.ports | array | 同步创建的 `two_wheel_port` 记录；未传 `ports` 时为空数组 |
| error | string | 失败原因 |

响应示例：

```json
{
  "success": true,
  "data": {
    "pile": {
      "id": "PILE_ID",
      "pile_no": "BKV001",
      "device_no": "BKV001",
      "device_mac": "A1:B2:C3:D4:E5:F6",
      "device_name": "东门1号二轮桩",
      "brand_model": "BKV-10K",
      "using_status": "using",
      "device_status": "offline",
      "charging_port_num": 2,
      "two_wheel_station_id": "STATION_ID",
      "station_name": "东门电站",
      "remark": "东门第一台"
    },
    "ports": [
      {
        "id": "PORT_ID_00",
        "port_no": "00",
        "max_power": 0.8,
        "port_status": "enable",
        "using_status": "free"
      }
    ]
  }
}
```

### POST /twoWheel/pile/update

修改二轮充电桩。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电桩修改 | v2 二轮充电桩修改 |
| --- | --- | --- |
| 前端接口 | `POST /charging/pile/update` | `POST /twoWheel/pile/update` |
| 数据落点 | `charging_pile`、`charging_port` | `two_wheel_pile` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮“按桩主键修改桩信息”的设计，`id` 必填，其他字段按需传入。 |
| 继承 | `pile_status=enable/disable`、`using_status`、桩名称、设备号、品牌、备注、运营方等字段继续可修改。 |
| 继承 | 当前实现已补齐四轮修改校验：设备号变更会查重，站点变更会校验站点存在，状态值必须合法，未传字段不会被默认值覆盖。 |
| 修改 | 接口名从 `/charging/pile/update` 换为 `/twoWheel/pile/update`。 |
| 修改 | 数据不再写 `charging_pile`，只更新 `two_wheel_pile`。 |
| 修改 | 四轮旧接口可在同一次桩修改里增删改 `charging_ports`；二轮 v2 将枪口增删改拆到 `/twoWheel/port/*`，桩接口只负责桩主数据。 |
| 修改 | 桩迁移站点时，同步刷新新旧站点统计，并更新下属 `two_wheel_port` 的站点冗余字段。 |
| 新增 | 可修改 BKV 二轮设备管理字段：`device_mac/brand_model/device_status/signal_strength/device_temperature/remote_control_enabled/realtime_query_enabled`。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 桩主键 |
| two_wheel_station_id | string | 否 | 所属站点 |
| device_no | string | 否 | 设备号/设备 UID |
| device_name | string | 否 | 设备名称 |
| device_mac | string | 否 | 设备 MAC |
| brand_model | string | 否 | 品牌型号 |
| pile_no | string | 否 | 桩编号 |
| name | string | 否 | 桩名称 |
| device_brand/brand | string | 否 | 设备品牌/品牌展示名 |
| type/device_type | string | 否 | 类型 |
| pile_status | string | 否 | `enable/disable` |
| using_status | string | 否 | `using/stock` |
| device_status | string | 否 | `online/offline/fault/configuring` |
| charging_port_num | number | 否 | 枪口数量 |
| operator_id/operator_name | string | 否 | 运营方 ID/名称 |
| signal_strength | number | 否 | 4G 信号等级 |
| device_temperature | number | 否 | 设备温度 |
| remote_control_enabled | boolean | 否 | 是否支持远程开启/关闭充电 |
| realtime_query_enabled | boolean | 否 | 是否支持远程查询实时状态 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的桩记录。

充电桩搜索和列表字段对照：

| 页面字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 设备编号/设备 UID | `two_wheel_pile.device_no` | 设备 UID 即 `device_no` |
| 设备名称 | `two_wheel_pile.device_name`、`two_wheel_pile.name` | 支持按名称搜索 |
| 设备 MAC | `two_wheel_pile.device_mac` | 可选 |
| 品牌型号 | `two_wheel_pile.brand_model` | 支持按品牌型号搜索 |
| 运营方 | `two_wheel_pile.operator_id`、`two_wheel_pile.operator_name` | 支持按运营方搜索 |
| 充电站 | `two_wheel_pile.two_wheel_station_id`、`two_wheel_pile.station_name` | 支持按站点搜索 |
| 在线/离线状态 | `two_wheel_pile.device_status` | 心跳约 10 分钟更新一次 |
| 最后在线/心跳时间 | `two_wheel_pile.last_heartbeat_at`、`two_wheel_pile.last_online_at` | 优先展示 `last_heartbeat_at` |
| 4G 信号等级 | `two_wheel_pile.signal_strength` | 可选 |
| 设备温度 | `two_wheel_pile.device_temperature` | 可选 |
| 插孔数量 | `two_wheel_pile.charging_port_num` | 创建时可由 `ports` 数组自动计算 |
| 远程开启/关闭充电 | `two_wheel_pile.remote_control_enabled` | 为 `true` 时展示操作 |
| 远程查询实时状态 | `two_wheel_pile.realtime_query_enabled` | 为 `true` 时展示操作 |

### POST /twoWheel/pile/status

单独修改二轮充电桩启停状态。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电桩状态修改 | v2 二轮充电桩状态修改 |
| --- | --- | --- |
| 前端接口 | `POST /charging/pile/switch` | `POST /twoWheel/pile/status` |
| 数据落点 | `charging_pile` | `two_wheel_pile` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮桩启停状态含义：`enable` 启用，`disable` 停用。 |
| 继承 | 当前实现已对齐四轮启停保护：桩必须归属启用站点；停用桩时若存在 `charging/occupy` 枪口则拒绝。 |
| 继承 | 当前实现已对齐四轮联动逻辑：启用桩会将下属枪口置为 `port_status=enable/using_status=free`，停用桩会将下属枪口置为 `port_status=disable/using_status=disable`。 |
| 修改 | 接口名从 `/charging/pile/switch` 换为 `/twoWheel/pile/status`。 |
| 修改 | 数据不再写 `charging_pile/charging_port`，只更新 `two_wheel_pile` 和下属 `two_wheel_port`。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 桩主键 |
| pile_status | string | 是 | `enable/disable` |

响应 `data` 返回更新后的桩记录。

### POST /twoWheel/pile/delete

删除二轮充电桩。后端按软删除处理，删除成功后会刷新所属站点的充电桩和插孔数量。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电桩删除 | v2 二轮充电桩删除 |
| --- | --- | --- |
| 前端接口 | `POST /charging/pile/destroy` | `POST /twoWheel/pile/delete` |
| 数据落点 | `charging_pile` | `two_wheel_pile` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮软删除设计。 |
| 继承 | 当前实现已对齐四轮删除保护：删除桩前会检查下属未删除枪口，存在枪口时拒绝删除。 |
| 修改 | 接口名从 `/charging/pile/destroy` 换为 `/twoWheel/pile/delete`。 |
| 修改 | 数据不再写 `charging_pile`，只删除 `two_wheel_pile`。 |
| 修改 | 二轮删除成功后会刷新所属 `two_wheel_station` 的桩数和插孔数。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 桩主键 |

请求示例：

```json
{
  "id": "PILE_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "PILE_ID"
  }
}
```

### POST /twoWheel/port/create

创建二轮枪口。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电枪口创建 | v2 二轮充电枪口创建 |
| --- | --- | --- |
| 前端接口 | 随 `POST /charging/pile/create` 或 `POST /charging/pile/update` 的 `charging_ports` 创建 | `POST /twoWheel/port/create` |
| 数据落点 | `charging_port` | `two_wheel_port` |

原四轮创建逻辑的核心流程：

1. 枪口创建嵌在四轮桩创建/修改流程里，前端传 `charging_ports` 数组。
2. 校验每个枪口 `device_no/max_power/port_status`。
3. 枪口编号按“桩编号 + 两位枪口序号”校验。
4. `port_status=enable` 时初始化 `using_status=free`，否则初始化为 `disable`。
5. 在同一事务内写入 `charging_port`，并关联 `charging_pile_id`。

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 保留枪口启停状态：`port_status=enable/disable`。 |
| 继承 | 保留枪口使用状态：空闲、充电中、故障等状态由设备回调和后台操作维护。 |
| 继承 | 仍然通过桩主键建立枪口归属；前端应传 `two_wheel_pile_id`。 |
| 继承 | 当前实现已补齐四轮核心校验：所属桩必须存在、`port_no` 必填、`port_status` 只能为 `enable/disable`、同一桩下端口号不能重复、枪口设备号不能重复。 |
| 修改 | 四轮没有独立 `port/create` 前端接口，枪口随桩一起维护；二轮 v2 独立出 `/twoWheel/port/create`，便于小程序和后台单独维护插孔。 |
| 修改 | 数据不再写 `charging_port`，只写 `two_wheel_port`。 |
| 修改 | 四轮 `charging_pile_id` 改为 `two_wheel_pile_id`，并冗余 `two_wheel_station_id/station_name/pile_name` 方便前端展示。 |
| 修改 | BKV 枪口号使用 `port_no`，0 号口传字符串 `"00"`；`device_no` 不传时由桩号和口号生成。 |
| 新增 | 增加 `device_brand/brand/brand_model/device_status/voltage/current/qrcode_id/options` 等设备展示和实时状态字段。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| two_wheel_pile_id | string | 是 | 所属桩 ID |
| port_no | string | 是 | 枪口号；BKV 0 号端口传 `"00"` |
| device_no | string | 否 | 枪口设备编号；不传时按桩号加枪口号生成 |
| pile_name | string | 否 | 充电桩名称；不传从桩带出 |
| two_wheel_station_id | string | 否 | 所属站点；不传从桩带出 |
| station_name | string | 否 | 充电站名称；不传从桩或站点带出 |
| device_brand | string | 否 | 设备品牌；不传从桩带出，默认 `BKV` |
| brand | string | 否 | 品牌展示名；不传从桩带出 |
| brand_model | string | 否 | 品牌型号；不传从桩带出 |
| device_status | string | 否 | 设备状态：`online/offline/fault/configuring`，默认从桩带出或 `offline` |
| port_status | string | 否 | `enable/disable`，默认 `enable` |
| using_status | string | 否 | `free/charging/fault`，默认 `free` |
| max_power | number | 否 | 最大功率 |
| voltage/current | number | 否 | 电压、电流 |
| qrcode_id | string | 否 | 二维码 ID |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "two_wheel_pile_id": "PILE_ID",
  "port_no": "00",
  "qrcode_id": "QRCODE_ID",
  "port_status": "enable",
  "using_status": "free",
  "max_power": 0.8,
  "remark": "0号口"
}
```

响应参数：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| data.id | string | 枪口主键 |
| data.device_no | string | 设备号 |
| data.port_no | string | 枪口号 |
| data.port_status | string | 端口启停状态 |
| data.using_status | string | 使用状态 |
| data.device_status | string | 设备在线/配置状态 |
| data.two_wheel_pile_id | string | 充电桩 ID |
| data.pile_name | string | 充电桩名称 |
| data.two_wheel_station_id | string | 充电站 ID |
| data.station_name | string | 充电站名称 |
| data.device_brand | string | 品牌 |
| data.brand_model | string | 品牌型号 |
| data.qrcode_id | string | 二维码 ID |
| data.remark | string | 备注 |
| data.created_at | string | 添加时间 |

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "PORT_ID",
    "device_no": "BKV00100",
    "port_no": "00",
    "port_status": "enable",
    "using_status": "free",
    "device_status": "offline",
    "two_wheel_pile_id": "PILE_ID",
    "pile_name": "东门1号二轮桩",
    "two_wheel_station_id": "STATION_ID",
    "station_name": "东门二轮充电站",
    "device_brand": "BKV",
    "brand_model": "BKV-10K",
    "qrcode_id": "QRCODE_ID",
    "remark": "0号口",
    "created_at": "2026-06-02 10:00:00"
  }
}
```

### POST /twoWheel/port/update

修改二轮枪口。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电枪口修改 | v2 二轮充电枪口修改 |
| --- | --- | --- |
| 前端接口 | 随 `POST /charging/pile/update` 的 `charging_ports` 修改 | `POST /twoWheel/port/update` |
| 数据落点 | `charging_port` | `two_wheel_port` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮“按枪口主键修改枪口信息”的底层模型，`id` 必填，其他字段按需传入。 |
| 继承 | `device_no/port_status/using_status/max_power/parking_space_arr/qrcode_id/remark/options` 等枪口属性继续可维护。 |
| 继承 | 当前实现已补齐四轮修改校验：端口状态必须合法，端口号和枪口设备号不能重复，迁移桩时目标桩必须存在。 |
| 修改 | 四轮枪口修改嵌在 `/charging/pile/update`；二轮 v2 独立为 `/twoWheel/port/update`。 |
| 修改 | 数据不再写 `charging_port`，只更新 `two_wheel_port`。 |
| 修改 | 四轮 `charging_pile_id` 改为 `two_wheel_pile_id`，可同步维护站点和桩展示冗余字段。 |
| 修改 | 枪口改状态时会同步 `using_status` 和 `options.logic_status`；迁移桩或站点时会刷新新旧桩/站统计。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 枪口主键 |
| port_no | string | 否 | 枪口号 |
| device_no | string | 否 | 设备号 |
| two_wheel_pile_id | string | 否 | 充电桩 ID |
| pile_name | string | 否 | 充电桩名称 |
| two_wheel_station_id | string | 否 | 充电站 ID |
| station_name | string | 否 | 充电站名称 |
| device_brand/brand/brand_model | string | 否 | 品牌、品牌展示名、品牌型号 |
| device_status | string | 否 | `online/offline/fault/configuring` |
| port_status | string | 否 | `enable/disable` |
| using_status | string | 否 | `free/charging/fault` |
| max_power/voltage/current | number | 否 | 功率、电压、电流 |
| qrcode_id | string | 否 | 二维码 ID |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的枪口记录。

### POST /twoWheel/port/status

单独修改二轮枪口启停状态。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电枪口状态修改 | v2 二轮充电枪口状态修改 |
| --- | --- | --- |
| 前端接口 | `POST /charging/port/switch` | `POST /twoWheel/port/status` |
| 数据落点 | `charging_port` | `two_wheel_port` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮枪口启停状态含义：`enable` 启用，`disable` 停用。 |
| 继承 | 当前实现已对齐四轮启停保护：所属桩必须存在且启用，归属站点必须启用；停用在用枪口时拒绝。 |
| 继承 | 当前实现已对齐四轮联动逻辑：启用枪口会置 `using_status=free`，停用枪口会置 `using_status=disable`，并同步 `options.logic_status`。 |
| 修改 | 接口名从 `/charging/port/switch` 换为 `/twoWheel/port/status`。 |
| 修改 | 数据不再写 `charging_port`，只更新 `two_wheel_port.port_status/using_status/options`。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 枪口主键 |
| port_status | string | 是 | `enable/disable` |

响应 `data` 返回更新后的枪口记录。

### POST /twoWheel/port/delete

删除二轮枪口。后端按软删除处理，删除成功后会刷新所属站点的插孔数量。

#### 四轮旧接口对照和二轮继承说明

| 对照项 | 原四轮充电枪口删除 | v2 二轮充电枪口删除 |
| --- | --- | --- |
| 前端接口 | `POST /charging/port/destroy` | `POST /twoWheel/port/delete` |
| 数据落点 | `charging_port` | `two_wheel_port` |

二轮 v2 对旧设计的继承和增改：

| 类型 | 内容 |
| --- | --- |
| 继承 | 沿用四轮软删除设计。 |
| 继承 | 当前实现已对齐四轮删除保护：枪口处于 `charging/occupy` 时拒绝删除。 |
| 修改 | 接口名从 `/charging/port/destroy` 换为 `/twoWheel/port/delete`。 |
| 修改 | 数据不再写 `charging_port`，只删除 `two_wheel_port`。 |
| 修改 | 二轮删除成功后会刷新所属 `two_wheel_station` 的插孔数量和所属 `two_wheel_pile.charging_port_num`。 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 枪口主键 |

请求示例：

```json
{
  "id": "PORT_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "PORT_ID"
  }
}
```

### POST /twoWheel/strategy/create

创建二轮计费策略。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 策略名称 |
| type | string | 是 | `duration/electricity_service/per_use` |
| strategy_no | string | 否 | 策略编号；不传后端生成 |
| status | string | 否 | `enable/disable`，默认 `enable` |
| data_owner_id | string | 否 | 数据归属 |
| price | number | 按 `type` | `duration` 时表示元/小时；`per_use` 时表示每次价格 |
| electric_price | number | 按 `type` | `electricity_service` 时必填，电单价，单位元/度 |
| service_price | number | 按 `type` | `electricity_service` 时必填，服务费，单位元/度 |
| duration_minutes | number | 按 `type` | `per_use` 时必填，每次可充电时长，单位分钟 |
| description | string | 否 | 策略描述 |
| created_by | string | 否 | 添加人 |
| rules | object | 否 | 计费规则 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

数据库字段对应：

| 表字段 | 说明 |
| --- | --- |
| `two_wheel_strategy.name` | 策略名称 |
| `two_wheel_strategy.type` | 收费模式：`duration` 时长收费，`electricity_service` 电价分离收费，`per_use` 按次收费 |
| `two_wheel_strategy.price` | `duration` 的元/小时价格；`per_use` 的每次价格 |
| `two_wheel_strategy.electric_price` | `electricity_service` 的电单价，单位元/度 |
| `two_wheel_strategy.service_price` | `electricity_service` 的服务费，单位元/度 |
| `two_wheel_strategy.duration_minutes` | `per_use` 的每次可充电时长，单位分钟 |
| `two_wheel_strategy.description` | 策略描述 |
| `two_wheel_strategy.created_by` | 添加人 |
| `two_wheel_strategy.created_at` | 添加时间 |
| `two_wheel_strategy.rules` | 可选扩展规则 |

策略列表字段对应：

| 列表字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 策略名称 | `two_wheel_strategy.name` | 后台展示名称 |
| 收费类型 | `two_wheel_strategy.type` | `duration` 时长收费，`electricity_service` 电价分离收费，`per_use` 按次收费 |
| 添加时间 | `two_wheel_strategy.created_at` | 创建记录时间 |
| 添加人 | `two_wheel_strategy.created_by` | 后台创建人 |
| 描述 | `two_wheel_strategy.description` | 策略说明 |

#### 时长收费 duration

选择 `type=duration` 时，前端表单展示价格字段，单位为元/小时。创建时必传 `price`。

```json
{
  "name": "二轮按时长收费",
  "type": "duration",
  "price": 2,
  "description": "按实际充电时长计费",
  "created_by": "ADMIN_ID",
  "remark": "每小时2元"
}
```

字段落库：

| 业务含义 | 数据库字段 | 示例 |
| --- | --- | --- |
| 策略名称 | `name` | `二轮按时长收费` |
| 收费模式 | `type` | `duration` |
| 元/小时价格 | `price` | `2.00` |
| 描述 | `description` | `按实际充电时长计费` |
| 添加人 | `created_by` | `ADMIN_ID` |

#### 电价分离收费 electricity_service

选择 `type=electricity_service` 时，前端表单展示电单价和服务费字段，单位均为元/度。创建时必传 `electric_price` 和 `service_price`。

```json
{
  "name": "二轮电价服务费分离",
  "type": "electricity_service",
  "electric_price": 0.8,
  "service_price": 0.2,
  "description": "电费与服务费分开计算",
  "created_by": "ADMIN_ID",
  "remark": "电费0.8元/度，服务费0.2元/度"
}
```

字段落库：

| 业务含义 | 数据库字段 | 示例 |
| --- | --- | --- |
| 策略名称 | `name` | `二轮电价服务费分离` |
| 收费模式 | `type` | `electricity_service` |
| 电单价元/度 | `electric_price` | `0.80000` |
| 服务费元/度 | `service_price` | `0.20000` |
| 描述 | `description` | `电费与服务费分开计算` |
| 添加人 | `created_by` | `ADMIN_ID` |

#### 按次收费 per_use

选择 `type=per_use` 时，前端表单展示每次可充电时长和每次价格。创建时必传 `duration_minutes` 和 `price`。

```json
{
  "name": "二轮按次收费",
  "type": "per_use",
  "duration_minutes": 240,
  "price": 3,
  "description": "固定时长按次收费",
  "created_by": "ADMIN_ID",
  "remark": "每次3元，可充240分钟"
}
```

字段落库：

| 业务含义 | 数据库字段 | 示例 |
| --- | --- | --- |
| 策略名称 | `name` | `二轮按次收费` |
| 收费模式 | `type` | `per_use` |
| 每次可充电时长，分钟 | `duration_minutes` | `240` |
| 每次价格 | `price` | `3.00` |
| 描述 | `description` | `固定时长按次收费` |
| 添加人 | `created_by` | `ADMIN_ID` |

响应 `data` 返回 `two_wheel_strategy` 记录。

### POST /twoWheel/strategy/update

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 策略主键 |
| name | string | 否 | 策略名称 |
| type | string | 否 | `duration/electricity_service/per_use` |
| status | string | 否 | `enable/disable` |
| price | number | 否 | `duration` 的元/小时价格，或 `per_use` 的每次价格 |
| electric_price | number | 否 | 电单价，单位元/度 |
| service_price | number | 否 | 服务费，单位元/度 |
| duration_minutes | number | 否 | 按次收费的每次时长，单位分钟 |
| description | string | 否 | 策略描述 |
| created_by | string | 否 | 添加人 |
| rules | object | 否 | 计费规则 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的策略记录。

### POST /twoWheel/strategy/delete

删除二轮计费策略。后端按软删除处理。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 策略主键 |

请求示例：

```json
{
  "id": "STRATEGY_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "STRATEGY_ID"
  }
}
```

### POST /twoWheel/whitelist/create

创建二轮白名单。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| mobile | string | 是 | 用户手机号 |
| station_ids | array | 按 `unlimited` | 生效充电站；`unlimited=false` 时必填 |
| unlimited | boolean | 否 | 是否不限充电站，即全局生效，默认 `false` |
| start_time | string | 否 | 起始有效时间 |
| end_time | string | 否 | 截止有效时间 |
| remark | string | 否 | 备注 |
| user_id | string | 否 | 用户 ID |
| name | string | 否 | 姓名 |
| status | string | 否 | `enable/disable`，默认 `enable` |
| is_free | boolean | 否 | 是否免支付，默认 `true` |
| source | string | 否 | `admin/import/api` |
| created_by | string | 否 | 创建人 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "mobile": "13800000000",
  "station_ids": ["STATION_ID"],
  "unlimited": false,
  "start_time": "2026-06-02 00:00:00",
  "end_time": "2026-12-31 23:59:59",
  "remark": "园区内部用户"
}
```

响应 `data` 返回白名单记录。

### POST /twoWheel/whitelist/update

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 白名单主键 |
| user_id/mobile/name | string | 否 | 身份信息 |
| status | string | 否 | `enable/disable` |
| is_free | boolean | 否 | 是否免支付 |
| unlimited | boolean | 否 | 是否所有站点适用 |
| station_ids | array | 否 | 适用站点 |
| start_time/end_time | string | 否 | 生效期 |
| updated_by | string | 否 | 更新人 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的白名单记录。

### POST /twoWheel/whitelist/delete

删除二轮白名单。后端按软删除处理。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 白名单主键 |

请求示例：

```json
{
  "id": "WHITELIST_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "WHITELIST_ID"
  }
}
```

### POST /twoWheel/monthCard/config/create

创建月卡套餐。

月卡套餐列表字段对应：

| 列表字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 月卡套餐名称 | `two_wheel_month_card_config.name` | 套餐名 |
| 生效场站 | `two_wheel_month_card_config.station_ids` | 可为空，表示不限制或由业务解释 |
| 状态 | `two_wheel_month_card_config.status` | `enable/disable` |
| 价格 | `two_wheel_month_card_config.price` | 套餐价格 |
| 时长 | `two_wheel_month_card_config.total_time_hours`、兼容 `total_time` | 前端优先展示小时 |
| 生效日期 | `two_wheel_month_card_config.effective_start_at/effective_end_at` | 套餐展示/购买生效日期 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 套餐名称 |
| status | string | 否 | `enable/disable`，默认 `enable` |
| original_price | number | 是 | 原价 |
| price | number | 是 | 套餐价格 |
| total_time_hours | number | 是 | 套餐总时长，单位小时；后端同步换算为 `total_time` 秒 |
| expire_type | string | 是 | 过期类型：`days` 套餐天数，`fixed_date` 套餐截止日期 |
| valid_days | number | 按 `expire_type` | `expire_type=days` 时必填，有效天数 |
| expire_at | string | 按 `expire_type` | `expire_type=fixed_date` 时必填，截止日期 |
| unlimited_purchase | boolean | 否 | 总限购数量是否不限购，默认 `true` |
| purchase_limit | number | 否 | 总限购数量；`unlimited_purchase=false` 时填写 |
| refundable | boolean | 否 | 是否可退，默认 `false` |
| display_scope | array | 否 | 展示范围 |
| station_ids | array | 否 | 生效场站 |
| effective_start_at | string | 否 | 生效开始日期 |
| effective_end_at | string | 否 | 生效结束日期 |
| debt_check_type | string | 否 | 历史欠费校验：`no_limit` 不限，`deny_debt` 有欠费不准买 |
| agreement_type | string | 否 | 用户协议：`default/custom`，默认 `default` |
| agreement_content | string | 否 | 自定义用户协议内容 |
| notice_enabled | boolean | 否 | 办理须知是否启用，默认 `false` |
| notice_content | string | 否 | 办理须知内容 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "name": "30天月卡套餐",
  "original_price": 120,
  "price": 99,
  "total_time_hours": 300,
  "expire_type": "days",
  "valid_days": 30,
  "unlimited_purchase": false,
  "purchase_limit": 1000,
  "refundable": true,
  "display_scope": ["xcx"],
  "station_ids": ["STATION_ID"],
  "effective_start_at": "2026-06-02 00:00:00",
  "effective_end_at": "2026-12-31 23:59:59",
  "debt_check_type": "deny_debt",
  "agreement_type": "default",
  "notice_enabled": true,
  "notice_content": "购买后仅限指定场站使用"
}
```

响应 `data` 返回套餐记录。

### POST /twoWheel/monthCard/config/update

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 套餐主键 |
| name/status/original_price/price | string/number | 否 | 套餐基础信息 |
| total_time_hours/total_time | number | 否 | 套餐时长 |
| expire_type/valid_days/expire_at | string/number | 否 | 过期配置 |
| unlimited_purchase/purchase_limit/refundable | boolean/number | 否 | 限购和退款配置 |
| display_scope | array | 否 | 展示范围 |
| station_ids | array | 否 | 生效场站 |
| effective_start_at/effective_end_at | string | 否 | 生效日期 |
| debt_check_type | string | 否 | 历史欠费校验 |
| agreement_type/agreement_content | string | 否 | 用户协议 |
| notice_enabled/notice_content | boolean/string | 否 | 办理须知 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的套餐记录。

### POST /twoWheel/monthCard/config/delete

删除二轮月卡套餐。后端按软删除处理。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 套餐主键 |

请求示例：

```json
{
  "id": "MONTH_CARD_CONFIG_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "MONTH_CARD_CONFIG_ID"
  }
}
```

### POST /twoWheel/monthCard/create

创建用户月卡。支付购买时先创建月卡，再调用 `/pay/twoWheelMonthCard`。

月卡用户列表字段对应：

| 列表字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 卡号 | `two_wheel_month_card.card_no` | 月卡编号 |
| 月卡套餐名称 | `two_wheel_month_card.config_name` | 创建时从套餐带出 |
| 状态 | `two_wheel_month_card.status` | `waiting/enable/used/expired` |
| 用户 | `two_wheel_month_card.user_id/user_name/mobile` | 用户信息 |
| 有效期 | `two_wheel_month_card.start_date/end_date` | 月卡有效期 |
| 是否首次购买 | `two_wheel_month_card.is_first_purchase` | 首购标识 |
| 剩余时长 | `two_wheel_month_card.left_time` | 单位秒，前端可换算小时 |
| 生效场站 | `two_wheel_month_card.station_ids` | 生效场站 |
| 已付金额 | `two_wheel_month_card.paid_fee` | 支付成功后累加 |
| 创建时间 | `two_wheel_month_card.created_at` | 创建记录时间 |
| 推荐人 | `two_wheel_month_card.recommender_id/recommender_name` | 推荐人信息 |
| 添加方式 | `two_wheel_month_card.add_source` | `admin/xcx/import/api` 等 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| config_id | string | 否 | 套餐 ID；传入后自动带出价格、时长、场站 |
| config_name | string | 否 | 月卡套餐名称；不传从套餐带出 |
| user_id | string | 是 | 用户 ID |
| user_name | string | 否 | 用户名称 |
| mobile | string | 否 | 手机号 |
| status | string | 否 | `waiting/enable/used/expired`，默认 `waiting` |
| price | number | 否 | 价格；不传从套餐带出 |
| paid_fee | number | 否 | 已付金额，默认 0 |
| total_time | number | 否 | 总时长，秒 |
| left_time | number | 否 | 剩余时长，秒 |
| start_date/end_date | string | 否 | 有效期 |
| station_ids | array | 否 | 生效场站 |
| recommender_id | string | 否 | 推荐人 ID |
| recommender_name | string | 否 | 推荐人名称 |
| add_source | string | 否 | 添加方式，默认 `admin` |
| user_remark | string | 否 | 用户备注 |
| remark | string | 否 | 后台备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回月卡记录，包含自动生成的 `card_no` 和 `is_first_purchase`。

### POST /twoWheel/monthCard/update

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 月卡主键 |
| status | string | 否 | `waiting/enable/used/expired` |
| left_time/total_time | number | 否 | 剩余/总时长 |
| start_date/end_date | string | 否 | 有效期 |
| station_ids | array | 否 | 生效场站 |
| user_remark/remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

响应 `data` 返回更新后的月卡记录。

### POST /twoWheel/monthCard/delete

删除用户月卡。后端按软删除处理。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 月卡主键 |

请求示例：

```json
{
  "id": "MONTH_CARD_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "MONTH_CARD_ID"
  }
}
```

### POST /twoWheel/rechargeCard/create

创建二轮充值卡。

充值卡展示字段对应：

| 展示字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 卡片编号 | `two_wheel_recharge_card.card_no` | 创建时必填 |
| 面额 | `two_wheel_recharge_card.denomination` | 卡面值 |
| 余额 | `two_wheel_recharge_card.balance` | 可用余额 |
| 绑定用户 | `two_wheel_recharge_card.bind_user_id/user_id/mobile` | 可选 |
| 创建时间 | `two_wheel_recharge_card.created_at` | 创建记录时间 |
| 备注 | `two_wheel_recharge_card.remark` | 可选 |

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| card_no | string | 是 | 卡片编号 |
| denomination | number | 是 | 面额；创建时余额默认等于面额 |
| user_id | string | 否 | 所属用户 |
| bind_user_id | string | 否 | 绑定用户；不传默认取 `user_id` |
| mobile | string | 否 | 手机号 |
| status | string | 否 | `enable/disable`，默认 `enable` |
| issue_status | string | 否 | `unbound/bound/active/returned`；有用户时默认 `bound` |
| issued_at | string | 否 | 发卡时间 |
| created_by | string | 否 | 创建人 |
| remark | string | 否 | 备注 |
| options | object | 否 | 扩展字段 |

请求示例：

```json
{
  "card_no": "TWRC0001",
  "denomination": 100,
  "bind_user_id": "USER_ID",
  "mobile": "13800000000",
  "remark": "线下售卡"
}
```

响应 `data` 返回充值卡记录，并写一条 `two_wheel_recharge_card_log`，类型为 `create`。

### POST /twoWheel/rechargeCard/update

修改二轮充值卡。只能修改备注和用户信息，其余字段不能通过该接口修改。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 充值卡主键 |
| user_id/bind_user_id/mobile | string | 否 | 用户信息 |
| updated_by | string | 否 | 更新人 |
| remark | string | 否 | 备注 |

响应 `data` 返回更新后的充值卡记录。

### POST /twoWheel/rechargeCard/recharge

对指定充值卡充值。充值成功后面额和余额同时增加本次充值金额，并写余额流水。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id/card_id | string | 是 | 充值卡主键 |
| amount | number | 是 | 充值金额，必须大于 0 |
| remark | string | 是 | 充值备注 |
| operator_id | string | 否 | 操作人 |
| trade_no | string | 否 | 外部交易号 |
| source | string | 否 | 来源，默认 `admin` |

请求示例：

```json
{
  "id": "RECHARGE_CARD_ID",
  "amount": 50,
  "remark": "后台充值"
}
```

响应 `data.card` 返回充值后的充值卡，`data.log` 返回 `type=recharge` 的余额流水。

### POST /twoWheel/rechargeCard/return

对指定充值卡做退回销账。输入本次需销账金额后，生成虚拟退款流水，面额和余额同时减少本次金额。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id/card_id | string | 是 | 充值卡主键 |
| amount | number | 是 | 本次销账金额，必须大于 0 且不能超过余额/面额 |
| remark | string | 是 | 退回备注 |
| operator_id | string | 否 | 操作人 |
| trade_no | string | 否 | 外部流水号 |
| source | string | 否 | 来源，默认 `admin` |

请求示例：

```json
{
  "id": "RECHARGE_CARD_ID",
  "amount": 30,
  "remark": "退回销账"
}
```

响应 `data.card` 返回退回后的充值卡，`data.log` 返回 `type=return_card` 的虚拟退款流水，流水 `options.virtual_refund=true`。

### POST /twoWheel/rechargeCard/logs

查询充值卡使用记录和余额流水。记录包含该卡刷卡消费产生的充电订单 `order_id`，以及充值、退回、消费等余额变动。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id/card_id | string | 是 | 充值卡主键 |

响应 `data` 返回 `two_wheel_recharge_card_log` 列表，常见 `type` 包括 `create/recharge/consume/return_card/manual_adjust`。

### POST /twoWheel/rechargeCard/delete

删除二轮充值卡。仅卡面值没有使用过时允许删除；有 `type=consume` 的消费流水，或余额与面额不一致时不允许删除。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 充值卡主键 |

请求示例：

```json
{
  "id": "RECHARGE_CARD_ID"
}
```

响应示例：

```json
{
  "success": true,
  "data": {
    "id": "RECHARGE_CARD_ID"
  }
}
```

## 5. 小程序业务接口

### POST /twoWheel/xcx/order/create

创建二轮充电订单。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| user_id | string | 是 | 用户 ID |
| pile_no | string | 否 | 桩编号；和 `device_no/two_wheel_port_id` 任选定位 |
| port_no | string | 否 | 枪口号；BKV 0 号口传 `"00"` |
| device_no | string | 否 | 枪口设备号 |
| two_wheel_port_id | string | 否 | 枪口 ID |
| strategy_id | string | 否 | 计费策略 |
| identity_source | string | 否 | `user/monthly_card/charging_card/whitelist`，默认 `user` |
| price | number | 否 | 订单金额 |
| original_price | number | 否 | 原始金额 |
| electric_price/service_price | number | 否 | 电费、服务费 |
| discount_fee | number | 否 | 优惠金额 |
| debt_fee | number | 否 | 欠费金额 |
| prepay_fee | number | 否 | 预付金额 |
| prepay_channel | string | 否 | 预付渠道 |
| change_fee | number | 否 | 找零 |
| supplement_fee | number | 否 | 补缴金额 |
| supplement_channel | string | 否 | 补缴渠道 |
| manual_refund_fee | number | 否 | 手工退款 |
| modified_price | number | 否 | 修改金额 |
| price_arr | array | 否 | 价格明细 |

响应 `data` 返回 `two_wheel_order`，初始 `status=waiting`、`pay_status=uncleared`。

充电订单数据库字段对应：

| 业务字段 | 数据库字段 | 说明 |
| --- | --- | --- |
| 开始时间 | `two_wheel_order.charging_time` | 实际开始充电时间 |
| 结束时间 | `two_wheel_order.finish_time` | 实际结束时间 |
| 订单状态 | `two_wheel_order.status` | `waiting/charging/finished/cancelled` 等 |
| 充电时长 | `two_wheel_order.charging_duration` | 单位秒 |
| 完成方式 | `two_wheel_order.finish_type` | 正常结束、用户停止、设备停止等 |
| 计费金额 | `two_wheel_order.price` | 本次应计费总金额 |
| 应收电费 | `two_wheel_order.electric_price` | 电费金额 |
| 应收服务费 | `two_wheel_order.service_price` | 服务费金额 |
| 已付金额 | `two_wheel_order.paid_fee` | 已支付总额 |
| 优惠金额 | `two_wheel_order.discount_fee` | 优惠、减免金额 |
| 欠费金额 | `two_wheel_order.debt_fee` | 未结清金额 |
| 预付金额 | `two_wheel_order.prepay_fee`、兼容 `pay_pre_fee` | 预付金额 |
| 预付渠道 | `two_wheel_order.prepay_channel` | 微信、支付宝、余额等 |
| 找零 | `two_wheel_order.change_fee` | 预付多余退回或找零金额 |
| 补缴金额 | `two_wheel_order.supplement_fee`、兼容 `pay_after_fee` | 后补支付金额 |
| 补缴渠道 | `two_wheel_order.supplement_channel` | 补缴支付渠道 |
| 手工退款 | `two_wheel_order.manual_refund_fee` | 后台手工退款 |
| 原金额 | `two_wheel_order.original_price` | 修改前原始金额 |
| 修改金额 | `two_wheel_order.modified_price` | 后台调整后的金额差额或调整金额 |
| 最近支付时间 | `two_wheel_order.latest_pay_time` | 最近一次支付成功时间 |

### POST /twoWheel/xcx/startCharge

手动启动订单。普通支付订单通常由支付成功回调触发；月卡、充值卡、白名单会在 `/pay/twoWheelCharging` 权益分支中触发。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| order_id | string | 是 | 订单 ID |
| billing_mode | string | 否 | `duration/electricity_service/per_use`，BKV 下发使用 |
| charge_time_min | number | 否 | 充电分钟数 |
| max_charge_time_min | number | 否 | 最大充电分钟数，默认 600 |
| total_amount_cent | number | 否 | 金额分，默认订单金额乘 100 |
| bkv_payload | object | 否 | 透传给 BKV 中间件的额外字段 |

响应参数：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| data | object | 订单记录 |
| send_to_device | boolean | 是否下发到设备 |
| device_response | object | BKV 中间件请求和响应 |

### POST /twoWheel/xcx/stopCharge

停止订单并结算权益。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| order_id | string | 是 | 订单 ID |
| stop_reason | string | 否 | 停止原因，默认 `manual` |
| charging_duration | number | 否 | 充电时长，秒 |
| charging_power | number | 否 | 充电电量 |
| price/original_price | number | 否 | 金额 |
| electric_price/service_price | number | 否 | 电费、服务费 |
| price_arr | array | 否 | 价格明细 |
| force | boolean | 否 | BKV 停止失败时是否强制本地结算，默认 `false` |
| bkv_payload | object | 否 | 透传给 BKV 中间件的额外字段 |

响应同启动接口。月卡订单扣 `left_time`；充值卡订单扣 `balance`。

## 6. 支付接口

### POST /pay/twoWheelCharging

二轮充电统一支付入口。通过 `identity_source` 决定普通支付、月卡、充值卡、白名单。

#### 普通支付

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| two_wheel_order_ids | array | 是 | 订单 ID 数组；也兼容 `order_ids/order_id/two_wheel_order_id` |
| identity_source | string | 否 | 普通支付传 `user` 或不传 |
| method | string | 是 | 支付方式，例如 `wx_pay/ali_pay` |
| pay_type | string | 否 | 支付场景，例如 `mini/manual` |
| user_id | string | 否 | 支付用户 |

响应包含 `trade_id/trade_no/total_price/data.pay_orders`。

#### 月卡充电

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| order_id | string | 是 | 订单 ID |
| identity_source | string | 是 | 固定 `monthly_card` |
| month_card_id | string | 是 | 月卡 ID |
| user_id | string | 是 | 用户 ID |

响应 `data.identity_source=monthly_card`，并返回订单和月卡。

#### 充值卡充电

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| order_id | string | 是 | 订单 ID |
| identity_source | string | 是 | `charging_card` 或 `recharge_card` |
| recharge_card_id | string | 是 | 充值卡 ID |
| user_id | string | 是 | 用户 ID |

响应 `data.identity_source=charging_card`，并返回订单和充值卡。

#### 白名单充电

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| order_id | string | 是 | 订单 ID |
| identity_source | string | 是 | 固定 `whitelist` |
| user_id | string | 否 | 用户 ID |
| mobile | string | 否 | 手机号；`user_id/mobile` 至少一个用于命中白名单 |

响应 `data.identity_source=whitelist`。

### POST /pay/twoWheelMonthCard

购买或续费二轮月卡。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| month_card_id | string | 是 | 月卡 ID；也兼容 `month_card_ids/card_id` |
| method | string | 是 | 支付方式 |
| pay_type | string | 否 | 支付场景 |
| user_id | string | 否 | 支付用户 |

响应包含 `trade_id/trade_no/total_price/data.pay_orders`。支付成功后月卡置为 `enable` 并写月卡流水。

### POST /pay/twoWheelChargingCard

二轮充值卡充值。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| recharge_card_id | string | 是 | 充值卡 ID；也兼容 `charging_card_id/card_id` |
| amount | number | 是 | 充值金额；也兼容 `price/denomination` |
| method | string | 是 | 支付方式 |
| pay_type | string | 否 | 支付场景 |
| user_id | string | 否 | 支付用户 |

响应包含 `trade_id/trade_no/total_price/data.pay_orders`。支付成功后充值卡 `denomination` 和 `balance` 同步增加。

## 7. 数据库表说明

二轮充电业务表统一使用 `two_wheel_*` 前缀。BKV 只作为 `device_brand='BKV'` 的设备品牌，不作为业务模块名。

| 表名 | 用途 | 关键字段 | 业务关系 |
| --- | --- | --- | --- |
| `two_wheel_station` | 二轮充电站 | `station_no`、`name`、`address`、`status`、`whitelist_apply`、`two_wheel_strategy_ids`、`charging_pile_num`、`charging_port_num` | 站点下挂多个二轮桩和插孔；通过 `two_wheel_strategy_ids` 绑定三种计费策略 |
| `two_wheel_pile` | 二轮充电桩 | `pile_no`、`device_no`、`device_mac`、`device_name`、`brand_model`、`two_wheel_station_id`、`device_status`、`charging_port_num` | 归属 `two_wheel_station`；一台桩可包含多个 `two_wheel_port` |
| `two_wheel_port` | 二轮充电口/插孔 | `port_no`、`device_no`、`two_wheel_pile_id`、`two_wheel_station_id`、`qrcode_id`、`max_power`、`port_status`、`using_status` | 归属桩和站点；小程序扫码、下单和设备启停都定位到插孔 |
| `two_wheel_strategy` | 二轮计费策略 | `name`、`type`、`price`、`electric_price`、`service_price`、`duration_minutes`、`description`、`created_by` | 支持 `duration` 时长收费、`electricity_service` 电价分离、`per_use` 按次收费 |
| `two_wheel_order` | 二轮充电订单 | `order_no`、`user_id`、`two_wheel_station_id`、`two_wheel_pile_id`、`two_wheel_port_id`、`status`、`charging_time`、`finish_time`、`price`、`paid_fee`、`refund_fee`、`trade_no`、`device_order_no` | 关联站、桩、口、策略和支付单；记录充电生命周期、金额、支付、退款、补缴和设备订单信息 |
| `two_wheel_whitelist` | 二轮白名单 | `mobile`、`station_ids`、`unlimited`、`start_time`、`end_time`、`status`、`remark` | 站点启用 `whitelist_apply=true` 且白名单命中时，可作为免支付权益通道 |
| `two_wheel_month_card_config` | 二轮月卡套餐 | `name`、`original_price`、`price`、`total_time_hours`、`expire_type`、`valid_days`、`expire_at`、`station_ids`、`debt_check_type`、`agreement_type`、`notice_enabled` | 后台配置套餐；用户购买后生成 `two_wheel_month_card` |
| `two_wheel_month_card` | 用户月卡 | `card_no`、`config_id`、`config_name`、`user_id`、`user_name`、`mobile`、`status`、`start_date`、`end_date`、`left_time`、`paid_fee`、`recommender_id`、`add_source` | 记录用户已购买月卡；充电结算可扣减剩余时长 |
| `two_wheel_month_card_log` | 月卡流水 | `card_id`、`order_id`、`pay_order_id`、`type`、`amount`、`before_left_time`、`after_left_time`、`trade_no` | 记录购买、续卡、充电扣时长、退款、后台调整等时长变化 |
| `two_wheel_recharge_card` | 二轮充值卡 | `card_no`、`denomination`、`balance`、`bind_user_id`、`mobile`、`status`、`remark` | 记录充值卡卡面值和余额；可绑定用户并作为充电支付权益 |
| `two_wheel_recharge_card_log` | 充值卡流水 | `card_id`、`order_id`、`pay_order_id`、`type`、`amount`、`before_balance`、`after_balance`、`before_denomination`、`after_denomination` | 记录创建、充值、消费、退回、调整等余额变化；消费记录可通过 `order_id` 关联充电订单 |
| `two_wheel_pay_order` | 二轮支付单 | `pay_no`、`target_type`、`target_id`、`method`、`status`、`price`、`paid_fee`、`trade_no`、`paid_at` | 二轮充电订单、月卡、充值卡的专用支付单；不复用四轮 `/pay/charging` |

所有 `two_wheel_*` 表都应包含 `id`、`created_at`、`updated_at`、`deleted_at`、`deleted`，用于主键、创建/更新时间和软删除。

## 8. 接口实现状态检查

当前文档已展开的提交类接口均能在代码中找到对应实现：

| 文档接口范围 | 当前实现状态 |
| --- | --- |
| `/twoWheel/station/*` | 已实现：`router.js -> controller.twoWheel.station* -> service.twoWheel.station*` |
| `/twoWheel/pile/*` | 已实现：`router.js -> controller.twoWheel.pile* -> service.twoWheel.pile*` |
| `/twoWheel/port/*` | 已实现：`router.js -> controller.twoWheel.port* -> service.twoWheel.port*` |
| `/twoWheel/strategy/*` | 已实现：`router.js -> controller.twoWheel.strategy* -> service.twoWheel.strategy*` |
| `/twoWheel/whitelist/*` | 已实现：`router.js -> controller.twoWheel.whitelist* -> service.twoWheel.whitelist*` |
| `/twoWheel/monthCard/config/*` | 已实现：`router.js -> controller.twoWheel.monthCardConfig* -> service.twoWheel.monthCardConfig*` |
| `/twoWheel/monthCard/*` | 已实现：`router.js -> controller.twoWheel.monthCard* -> service.twoWheel.monthCard*` |
| `/twoWheel/rechargeCard/*` | 已实现：创建、修改、删除、详情、列表、充值、退回、流水均已接入 `controller.twoWheel` 和 `service.twoWheel` |
| `/twoWheel/xcx/order/create` | 已实现：`router.js -> controller.xcxTwoWheel.createOrder -> service.twoWheel.createOrder` |
| `/twoWheel/xcx/startCharge` | 已实现：`router.js -> controller.xcxTwoWheel.startCharge -> service.twoWheel.startCharge` |
| `/twoWheel/xcx/stopCharge` | 已实现：`router.js -> controller.xcxTwoWheel.stopCharge -> service.twoWheel.stopCharge` |
| `/pay/twoWheelCharging` | 已实现：`controller.pay.payForTwoWheelCharging -> service.twoWheel.payTwoWheelCharging`，支付回调接入 `finishTwoWheelTrade` |
| `/pay/twoWheelMonthCard` | 已实现：`controller.pay.payForTwoWheelMonthCard -> service.twoWheel.payTwoWheelMonthCard`，支付回调接入 `finishTwoWheelTrade` |
| `/pay/twoWheelChargingCard` | 已实现：`controller.pay.payForTwoWheelChargingCard -> service.twoWheel.payTwoWheelChargingCard`，支付回调接入 `finishTwoWheelTrade` |

额外已存在但本文未逐项展开的查询/状态接口：

| 接口 | 当前状态 |
| --- | --- |
| `/twoWheel/*/detail`、`/twoWheel/*/list` | router 已存在，统一走 `service.twoWheel.detailEntity/listEntity` |
| `/twoWheel/order/list`、`/twoWheel/order/detail`、`/twoWheel/order/logs` | router 已存在，用于后台订单查询和流水查询 |
| `/twoWheel/xcx/scanInfo` | router 已存在，用于小程序扫码获取站、桩、口和权益信息 |
| `/twoWheel/xcx/order/status`、`/twoWheel/xcx/order/logs` | router 已存在，用于小程序订单状态和日志查询 |
| `/twoWheel/xcx/monthCard/configs`、`/twoWheel/xcx/monthCard/my` | router 已存在，用于小程序月卡套餐和我的月卡 |
| `/twoWheel/xcx/rechargeCard/my` | router 已存在，用于小程序我的充值卡 |
| `/twoWheel/xcx/whitelist/status` | router 已存在，用于小程序白名单权益判断 |

实现说明：

- 小程序控制器 `controller/xcxTwoWheel.js` 复用 `service.twoWheel`，当前没有独立 `service/xcxTwoWheel.js`。
- 当前文档中已展开的接口均有对应 router/controller/service 实现。
- 查询类接口目前是“路由已存在，本文不展开详细请求响应”的状态，符合前述“查询接口可以不用写”的口径。

## 9. 当前代码和数据库核对

已对齐：

| 要求 | 当前实现 |
| --- | --- |
| 站点使用状态为正常收费、施工维护、关闭 | `two_wheel_station.status` 使用 `enable/repair/disable`，沿用四轮状态含义，创建/修改/状态接口会校验 |
| 创建或修改站点可指定白名单策略 | `whitelist_apply` 已在模型、SQL、服务、文档中支持 |
| 创建或修改站点可指定三种收费模式 | `duration_strategy_id/electricity_service_strategy_id/per_use_strategy_id` 会归一化写入 `two_wheel_strategy_ids` |
| 备注和说明可选 | 站点支持 `remark/comment/operating_description/options`；桩、枪、策略、权益表支持 `remark/options` |
| BKV 启停对齐中间件 | BKV 设备启动调用 `/bkv/remotecontrolboot`，停止调用 `/bkv/remotecontrolend` |

注意：

`/twoWheel/*/list/detail/logs` 查询接口仍存在，但本文不展开。前端需要查询时按现有路由调用即可。
